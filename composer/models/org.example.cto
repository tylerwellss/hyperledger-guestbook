namespace org.guestbook

asset Entry identified by entryId {
  o String entryId
  o String message
  o String name
  o DateTime date
}

transaction Sign {
  o String message
  o String name
  o String entryId
}